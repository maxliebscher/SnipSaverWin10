# v0.5.0

- Language fallback now resolves `exact locale -> base locale -> en`.
- Tray/settings localization remains separate from logs and filenames.
- Four icon themes remain selectable in settings and previewable in the HTML gallery.
- Arabic keeps RTL support in settings while reset language remains always visible.
