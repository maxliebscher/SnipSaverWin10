# v0.3.0

- Settings now use a single JSON config model for `JPG/PNG`, quality, target folder, autostart, language, capture toggle, and icon theme.
- PNG is treated as a first-class output option.
- Reset defaults and reset language are separated.
- Tray and settings operate on the same live config state.
