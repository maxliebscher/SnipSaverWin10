# SnipSaver v0.5.7

- disables the unstable multilingual UI path and limits the visible language selector to Deutsch/English
- resets unsupported saved language values back to system language with German fallback
- removes the experimental Settings launcher build artifacts
- keeps the tray icon on the real `.ico` assets
- restarts the tray app with clean German text and verifies the clipboard loop guard remains quiet
