# SnipSaver v0.5.8

- regenerates the active tray `.ico` from the exact `icon-previews/clean-capture.svg` geometry
- keeps the paused tray `.ico` on the same shape language with muted colors
- keeps the runtime tray icon loading from the real `.ico` assets
- verifies German UI text, icon loading, autostart entries, process count, and clipboard loop guard
- adds `Test-SnipSaverSettings.ps1` for repeatable automated settings coverage
