# SnipSaver v0.5.6

- fixes a manual-language settings crash by using stable .NET array lookup
- keeps the language dropdown behavior where selecting a concrete language switches to manual mode
- records that Computer Use bootstrap failed in this session before UI automation could run
- makes German and French UI strings ASCII-stable for Windows PowerShell 5.1 WinForms
- adds a SettingsOnly startup mode so the real settings window can be opened and tested directly
- prevents autostart write failures from closing the settings UI
