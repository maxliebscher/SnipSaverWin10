# v0.5.1

- `clean-capture` now uses a brighter teal signal color so it stands out better in the Windows tray.
- Active and paused tray states are more visually distinct.
- Live startup has been aligned to the same installed tray launcher used by Windows autostart.
