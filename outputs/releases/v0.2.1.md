# v0.2.1

- Shared core introduced for capture, config, translations, autostart, and icon generation.
- Tray and headless modes no longer share the same startup path by accident.
- Capture mutex and tray mutex are separated to prevent duplicate capture and duplicate tray instances.
- Startup now targets the tray app consistently.
