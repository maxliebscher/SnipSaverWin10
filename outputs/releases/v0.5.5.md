# SnipSaver v0.5.5

- adds real tray `.ico` assets for active and paused states
- loads the `.ico` assets in the tray instead of relying only on runtime drawing
- fixes language selection so choosing a concrete language immediately switches to manual mode
- fixes UTF-8 locale loading under Windows PowerShell 5.1
- verifies the restarted tray app, current-user autostart entries, and clipboard loop guard
