# SnipSaver v0.5.2

- clarified tray wording from capture actions to service actions
- added clipboard settings with default keep-in-clipboard behavior and selectable `PNG` or `JPG` clipboard output
- fixed the settings window layout so lower controls and action buttons are no longer clipped
- simplified the default `clean-capture` icon into a clearer image-style glyph while keeping the brighter teal tray color
- keeps the installed tray launcher as the live and autostart path
